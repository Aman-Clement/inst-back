{
  "name": "instagram",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "deploy":"echo 'run deploy'",
    "build": "npm install && npm run clean && npm run build:server",
    "build:server": "npm run lint && npm run test && npm run compile",
    "lint": "eslint .",
    "test": "jest",
    "compile": "tsc",
    "clean": "rm -rf ./dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "gh-pages": "^5.0.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.9.1",
    "nodemon": "^2.0.20"
  }
}
